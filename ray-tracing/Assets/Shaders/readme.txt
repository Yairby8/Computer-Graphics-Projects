* Q4: intersectPlaneCheckered implementation: We implemented the function by first intersecting the ray with the plane using the standard ray-plane formula (rejecting parallel rays, hits with t <= EPS, and hits farther than the current bestHit). On a valid hit we store distance, position, and the plane normal. To create the checkerboard, we scale the hit position by 2.0 so each grid cell corresponds to 0.5 world units, take floor of the coordinates to get integer cell indices, and choose between m1 and m2 based on the parity (even/odd) of the cell index sum, producing the required alternating checker pattern.

* Q5: intersectCylinderY implementation: We implemented the function by intersecting the ray with the cylinder’s side surface using the implicit infinite Y-aligned cylinder equation as defined in the question, which yields a quadratic in t. For each valid root in front of the ray, we check whether the corresponding hit point’s y coordinate lies within the finite height range we define and only keep the hits that do. Then we test the top and bottom caps by calling intersectCircle and update the best hits and normals similarly.